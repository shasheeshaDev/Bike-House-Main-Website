# Same Sanity project and dataset as the frontend, described again with the
# SANITY_STUDIO_ prefix — the frontend's NEXT_PUBLIC_* vars are not visible to
# the Studio bundle.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
SANITY_STUDIO_API_VERSION=2025-04-16
